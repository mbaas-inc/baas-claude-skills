{
  "name": "@mbaas/baas-web-sdk",
  "version": "0.7.0",
  "private": true,
  "description": "런타임 CDN 로드 BaaS 프론트 SDK (부패 방지 계층) — transport/훅을 한 곳에 두고 O(1) 전파",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.mjs",
    "deploy": "node deploy.mjs",
    "promote": "node promote.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "@types/react": "^18.3.0"
  }
}
